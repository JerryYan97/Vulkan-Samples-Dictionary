#version 450

layout (local_size_x = 4, local_size_y = 4) in;
layout (binding = 0, std140) uniform i1 {
    float arrayIn[16];
};
layout (binding = 2, std430) buffer o1 {
    float arrayOut[16];
};

void main()
{
    uint g_id = gl_LocalInvocationIndex;
    float inEle = arrayIn[g_id];
    arrayOut[g_id] = inEle * inEle;
}
